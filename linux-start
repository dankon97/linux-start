# 📘 Базовые команды Linux

## Навигация
- `ls -l` — список файлов
- `cd /path` — перейти в папку

## Работа с пакетами (Debian)
- `sudo apt update && sudo apt upgrade` — обновление системы
- `sudo apt install htop` — установка пакета

## Пользователи
- `adduser имя` — создать пользователя
- `passwd имя` — задать пароль

## Службы
- `systemctl status ssh` — статус сервиса SSH
- `systemctl enable apache2` — автозапуск Apache
